import React, { Component } from 'react';
import styles from './WorkView.sass';

class WorkView extends Component {
	render() {
		return (
			<div className={this.props.className}>
				Work View
			</div>
		)
	}
}

export default WorkView;